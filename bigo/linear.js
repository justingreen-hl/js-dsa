// O(n)
// Linear time complexity, running time increases linearly with the size of the input data
// think of counting from 1 to 100;

const arr = [1,2,3,4,5,6,7];

for(let i = 0; i < arr.length; i++) {
  console.log(arr[i]);
}