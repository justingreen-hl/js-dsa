const characters = ["Bob", "Linda", "Gene", "Tina", "Louise"];

// Print characters out 1 by 1
for(let i = 0; i < characters.length; i++) {
  //console.log(characters[i])
  //if(characters[i] === "Gene") return; // early termination - stops the loop when a condition is met
}

// 2d array (matrix), array of arrays
const matrix = [
  [1,2],
  [3,4]
];

console.log(matrix[0][1]); // access 2 in the first array
console.log(matrix[1][0]); // access 3 in the second array